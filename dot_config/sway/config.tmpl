# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).



# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- ./scripts/fuzzy-i3lock.sh
# exec --no-startup-id waybar

set $lock swaylock -c 1F1F28
# exec --no-startup-id swayidle \
#     timeout 600 '$lock' \
#     timeout 570 'swaymsg "output * dpms off"' \
#     resume 'swaymsg "output * dpms on"' before-sleep '$lock'

exec_always --no-startup-id ./scripts/autostart-wayland.sh
# exec_always --no-startup-id feh --bg-scale ./Pictures/autumn.png
# exec_always --no-startup-id feh --bg-scale ./Pictures/wallpapers/robot_3.jpg
# exec_always --no-startup-id feh --bg-scale ./Pictures/wallpapers/spirited-away.png
# exec_always --no-startup-id feh --bg-scale ./Desktop/chess.png

output * bg ~/Pictures/wallpapers/elden-ring.jpg fill

# NetworkManager is the most popular way to manage wireless networks on {{ .chezmoi.kernel.ostype }},
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec_always --no-startup-id ./scripts/autostart-x.sh

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set 'Master' 5%+ && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id amixer set 'Master' 5%- && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym XF86AudioNext exec --no-startup-id dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Next
bindsym XF86AudioPrev exec --no-startup-id dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.Previous
bindsym XF86AudioPlay exec --no-startup-id dbus-send --print-reply --dest=org.mpris.MediaPlayer2.spotify /org/mpris/MediaPlayer2 org.mpris.MediaPlayer2.Player.PlayPause

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec "kitty -e ./scripts/tmux-resume-or-create.sh"

# kill focused window
bindsym --to-code $mod+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run

# bindsym $mod+i exec "rofi -show combi -combi-modi drun,run -modi combi -theme gruvbox-{{ .chezmoi.group }}-soft"
# bindsym $mod+i exec "rofi -show combi -combi-modi drun,run -modi combi"
bindsym --to-code $mod+i exec "wofi --show drun"

# bindsym $mod+Shift+o exec "./scripts/fuzzy-i3lock.sh"
bindsym --to-code $mod+Shift+o exec $lock
bindsym --to-code $mod+Shift+p exec ./scripts/switch-display-input.sh toggle

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym --to-code $mod+h focus left
bindsym --to-code $mod+j focus down
bindsym --to-code $mod+k focus up
bindsym --to-code $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym --to-code $mod+Shift+h move left
bindsym --to-code $mod+Shift+j move down
bindsym --to-code $mod+Shift+k move up
bindsym --to-code $mod+Shift+l move right

# split in horizontal orientation
bindsym --to-code $mod+s split h

# split in vertical orientation
bindsym --to-code $mod+v split v

# enter fullscreen mode for the focused container
bindsym --to-code $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym --to-code $mod+t layout stacking
bindsym --to-code $mod+w layout tabbed
bindsym --to-code $mod+e layout toggle split

# toggle tiling / floating
bindsym --to-code $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym --to-code $mod+space focus mode_toggle

# focus the parent container
bindsym --to-code $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
# set $ws1 "1:i"
# set $ws2 "2:ii"
# set $ws3 "3:iii"
# set $ws4 "4:iv"
# set $ws5 "5:v"
# set $ws6 "6:vi"
# set $ws7 "7:vii"
# set $ws8 "8:viii"
# set $ws9 "9:ix"
# set $ws10 "10:x"

set $ws1 "1:бір"
set $ws2 "2:екі"
set $ws3 "3:үш"
set $ws4 "4:төрт"
set $ws5 "5:бес"
set $ws6 "6:алты"
set $ws7 "7:жеті"
set $ws8 "8:сегіз"
set $ws9 "9:тоғыз"
set $ws10 "10:он"

set $ws1 "1:1"
set $ws2 "2:2"
set $ws3 "3:3"
set $ws4 "4:4"
set $ws5 "5:5"
set $ws6 "6:6"
set $ws7 "7:7"
set $ws8 "8:8"
set $ws9 "9:9"
set $ws10 "10:10"

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"


# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1 ; workspace $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2 ; workspace $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3 ; workspace $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4 ; workspace $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5 ; workspace $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6 ; workspace $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7 ; workspace $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8 ; workspace $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9 ; workspace $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10 ; workspace $ws10

# reload the configuration file
bindsym --to-code $mod+Shift+r reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym --to-code $mod+Shift+c restart

bindsym --to-code $mod+Tab focus right

# exit i3 (logs you out of your X session)
bindsym --to-code $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Sway session.' -B 'Yes, exit wayland' 'swaymsg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym --to-code h resize shrink width 20 px or 20 ppt
        bindsym --to-code Shift+h resize grow left 20 px or 20 ppt
        bindsym --to-code l resize grow right 20 px or 20 ppt
        bindsym --to-code Shift+l resize shrink left 20 px or 20 ppt
        bindsym --to-code k resize shrink down 20 px or 20 ppt
        bindsym --to-code Shift+k resize grow up 20 px or 20 ppt
        bindsym --to-code j resize grow down 20 px or 20 ppt
        bindsym --to-code Shift+j resize shrink up 20 px or 20 ppt

        bindsym --to-code $mod+h focus left
        bindsym --to-code $mod+j focus down
        bindsym --to-code $mod+k focus up
        bindsym --to-code $mod+l focus right

        bindsym --to-code w move up 20 px
        bindsym --to-code a move left 20 px
        bindsym --to-code s move down 20 px
        bindsym --to-code d move right 20 px

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym --to-code $mod+r mode "default"

        bindsym $mod+Tab focus right
}

bindsym --to-code $mod+r mode "resize"

# passthrough mode for temporarily disabling shortcuts
bindsym --to-code $mod+p mode "passthrough"
mode "passthrough" {
        bindsym --to-code $mod+q mode "default"
}

# using https://github.com/greshake/i3status-rust as the status bar
# bar {
#     # font pango:Cascadia Mono PL:style=Regular, Font Awesome 5 Free 9
#     font pango:Cascadia Code PL:style=ExtraLight, Font Awesome 5 Free 9
#     # position top
#     # status_command i3status-rs .config/i3/status.toml
#     # i3bar_command i3bar --transparency

#     # height 19
#     # separator_symbol "/"

#     colors {
#         background #26262699
#         statusline #ffffff00
#         separator #66666600

#         # focused_workspace  #4c7899 #339966 #fff3b0
#         focused_workspace  #4c7899 #336666 #fff3b0
#         active_workspace   #333333 #fff3b0 #3d405b
#         inactive_workspace #333333 #424242 #ffffff
#         urgent_workspace   #2f343a #e09f3e #ffffff
#         binding_mode       #2f343a #e09f3e #ffffff
#     }


#     strip_workspace_numbers no
# }

# for_window [class=".*"] border pixel 0
for_window [class="ghidra-Ghidra"] floating enable
for_window [class="jetbrains-studio" title="win0"] floating enable
for_window [app_id="firefox"] border pixel 0
for_window [app_id="chromium"] border pixel 1
for_window [app_id="kitty"] border pixel 1
for_window [app_id="telegramdesktop"] border pixel 1
for_window [class="Steam"] border pixel 1
for_window [class="Spotify"] border pixel 1
for_window [class="Blender"] border pixel 1
for_window [class="vlc"] border pixel 1
for_window [app_id="pavucontrol"] floating enable
# for_window [class="Spotify"] border pixel 1
# for_window [class="qBittorrent"] border pixel 1
# for_window [class="Gimp-2.10"] border pixel 1
for_window [class=".*"] inhibit_idle fullscreen
for_window [app_id=".*"] inhibit_idle fullscreen

focus_on_window_activation focus

gaps inner 3

# Mouse configuration
input 1133:16511:Logitech_G502 {
    # left_handed enabled
    # tap enabled
    # natural_scroll disabled
    # dwt enabled
    accel_profile "flat" # disable mouse acceleration (enabled by default; to set it manually, use "adaptive" instead of "flat")
    pointer_accel -0.72 # set mouse sensitivity (between -1 and 1)
}

input "type:keyboard" {
    xkb_layout us,ru
    xkb_options grp:alt_shift_toggle
    repeat_delay 450
    repeat_rate 40
}

output DP-1 scale 1.4
bar swaybar_command waybar
